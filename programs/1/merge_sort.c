#include <stdio.h>

// Merge sort:
// - divide into n 'lists' of 1 elements
// - merge adjecent lists until one list is left
double *merge_sort(double arr[], const int arr_length) { return arr; }

int main() {
  double arr[11] = {4.2, 2.3, 3.8, 1.2, 9.4, 9.3, 3.2, 4.1, 4.1, 11.9};
}
